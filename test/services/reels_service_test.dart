import 'package:boxalltv/services/reels_service.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('ReelsService should instantiate', () {
    final service = ReelsService();
    expect(service, isNotNull);
  });
}
